<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="1dp"> <!-- فاصل 1 بيكسل بين الصور -->

    <!-- الصورة الأساسية (Thumbnail) -->
    <ImageView
        android:id="@+id/image_media"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#E0E0E0"
        android:scaleType="centerCrop"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- أيقونة الفيديو (تظهر فقط لو الملف فيديو) -->
    <ImageView
        android:id="@+id/icon_video"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:src="@android:drawable/ic_media_play"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:tint="#FFFFFF" />

    <!-- طبقة التظليل عند التحديد (اختياري لاحقاً) -->
    <View
        android:id="@+id/overlay_selected"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#4D000000"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/image_media"
        app:layout_constraintEnd_toEndOf="@id/image_media"
        app:layout_constraintStart_toStartOf="@id/image_media"
        app:layout_constraintTop_toTopOf="@id/image_media" />

</androidx.constraintlayout.widget.ConstraintLayout>